# Pflicht
DATABASE_URL=postgresql://user:pass@host:5432/db?schema=public

# Optional: Speicherpfad für Datei-Blobs (existiert oder wird erstellt)
FILES_DIR=/var/data

# Optional: Gesamtes erlaubtes Nutzungsbudget für Dateien (Bytes) – Default 10737418240 (10 GiB)
DISK_TOTAL_BYTES=10737418240

# Optional: Rate Limiting (Token Bucket pro IP)
RATE_LIMIT_TOKENS=30
RATE_LIMIT_WINDOW_MS=60000

# Optional: Admin Auth (mindestens eine Variante; HASH hat Priorität wenn beide gesetzt)
# ADMIN_TOKEN=meinplaintextadmintoken
# ADMIN_TOKEN_HASH=hexsha256vonplaintext
# Hash erzeugen (Linux/macOS): echo -n "meinplaintextadmintoken" | openssl dgst -sha256 -binary | xxd -p -c 256
# Hash erzeugen (PowerShell): "meinplaintextadmintoken" | ForEach-Object { $_ } | openssl dgst -sha256 -binary | xxd -p -c 256

# NICHT setzen: interne Next.js Variablen (NEXT_*, __NEXT_*, VERCEL_*) – werden automatisch bereitgestellt / nicht benötigt