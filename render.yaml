services:
  - type: web
    name: upload-system-clean-v2
    runtime: node
    rootDir: apps/backend-clean
    buildCommand: npm install && npm run build
    startCommand: npm start
    autoDeployTrigger: commit
    plan: free
    region: oregon
    envVars:
      - key: NODE_ENV
        value: production